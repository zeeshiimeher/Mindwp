/**
 * Domain alias for `SplitHeroSection` with unchanged props.
 * Keeps feature page composition naming consistent.
 */
export { SplitHeroSection as FeatureHeroSection } from '@/components/reusable/single/SplitHeroSection';
