/**
 * Domain alias for `SimpleHero` with unchanged props.
 * Keeps industry page composition naming consistent.
 */
export { SimpleHero as IndustryHeroSection } from '@/components/reusable/single/SimpleHero';
