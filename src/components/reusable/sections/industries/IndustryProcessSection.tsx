/**
 * Domain alias for `ProcessStepsSection` with unchanged props.
 * Used when category pages need a higher-level mapping flow.
 */
export { ProcessStepsSection as IndustryProcessSection } from '@/components/reusable/sections/core/ProcessStepsSection';
