/**
 * Domain alias for `TierCardsSection` with unchanged props.
 * Used when category pages need implementation pathways instead of package framing.
 */
export { TierCardsSection as IndustryPathwaysSection } from '@/components/reusable/sections/core/TierCardsSection';
