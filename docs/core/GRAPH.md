# Graph

Graph, authority, related-content ranking, and report artifacts are deferred while the new business-first page system is being rebuilt.

Use simple route/page metadata only where current runtime needs it. Do not require graph completeness before page design.

Later, after approved designs exist, rebuild graph and related-content rules around the final page system.

Do not restore:

- generated authority reports
- graph validators
- report manifests
- required related-section injection
- content graph completeness gates
- authority-score thresholds
